
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если Параметры.Ключ.Пустой() Тогда
		Если НЕ ЗначениеЗаполнено(Запись.Организация) Тогда
			Запись.Организация = НастройкиПользователяСервер.ЗначениеПоУмолчанию("ОсновнаяОрганизация");
		КонецЕсли;
		
		Если Запись.СтавкаФБ+Запись.СтавкаСубъектРФ=0 Тогда
			Запись.СтавкаФБ = 3;
			Запись.СтавкаСубъектРФ = 17;
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры
